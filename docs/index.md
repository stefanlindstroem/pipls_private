# Pi-PLS documentation

Pi-PLS is a multivariate linear-regression method for predicting one or more responses from a block
of predictors. It represents the predictive relation through paired predictor and response latent
variables.

## Start with the Pulp tutorial

The recommended entry point is [Pulp: a complete Pi-PLS workflow](tutorials/pulp.md). It develops one
real-data model from loading the matrices through:

- constructing a scikit-learn pipeline;
- evaluating CV-MSE across component counts;
- choosing the component count and its associated predictor rank;
- fitting the fixed model;
- generating selection-conditioned out-of-fold predictions;
- interpreting scores, loadings, Pi-PLS factors, coefficients, and prediction diagnostics one figure
  at a time.

Every tutorial figure is generated by the repository from the same executable workflow used by the
maintained Pulp example.

## Method in brief

Two parameters control model complexity:

- `n_components` is the number of paired latent variables retained for prediction;
- `predictor_rank` is the dimension of the predictor subspace retained before those pairs are
  estimated.

`PiPLSPathCV` evaluates successive component counts by cross-validation. For each count it selects a
predictor rank and reports response-standardized CV-MSE. The final model is normally chosen near an
elbow or plateau in the CV-MSE curve and then fitted explicitly with both ranks. See the
[theory overview](theory.md) for the mathematical construction.

## Other routes through the documentation

- [Quickstart](quickstart.md): literal NumPy matrices and one fixed fit without parameter selection.
- [Parameter selection](parameter_selection.md): the general path-search and rank-selection rules.
- [Model inspection](model_inspection.md): general definitions for fitted quantities and plots.
- [Examples](examples.md): synthetic, comparison, Sugarcane, Tobacco, and complete analysis scripts.
- [Estimator API](estimator_api.md): estimator parameters, fitted attributes, and scikit-learn
  behavior.
- [API reference](api/index.md): generated signatures and method contracts.
- [Preprocessing](preprocessing.md): model-internal scaling and leakage boundaries.
- [Cross-validation](cross_validation.md): splitters, OOF predictions, and validation provenance.
- [Datasets](datasets.md): repository data and synthetic generators.
- [Reproducibility](reproducibility.md): software, dataset, and generated-documentation controls.
