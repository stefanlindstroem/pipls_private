# Test protocol

1. Run focused tests while developing.
2. Run `make check` before delivery.
3. Run `make build` for packaging changes.
4. Record every command as passed, failed, or not run.
5. Never describe inspection alone as validation.
