Read `.llm/README.md`, `.llm/state.md`, `.llm/strategy.md`, and the relevant contracts first.
Confirm that the request is the current increment or an explicit owner-directed reordering.
Implement one bounded feature, add focused tests and documentation, avoid unrelated refactoring,
and for dataset/example work preserve the explicit `X`/`Y` reading contract in `.llm/data_io.md`,
update `.llm/state.md` when phase or public scope changes, and return one root-relative unified Git
patch plus validation results and direct Git commands.
