Read `.llm/README.md`, `.llm/state.md`, `.llm/development.md`, and the relevant contracts first.
Reproduce the stated defect against the uploaded snapshot and identify the public or internal
contract it violates. Fix it with the smallest coherent change, add a regression test at the
appropriate API boundary, update affected contracts/documentation, and return one root-relative
unified Git patch plus validation results and direct Git commands.
