Read `.llm/theory.md`, `.llm/mathematics.md`, `.llm/numerical_contracts.md`, and the relevant
accepted decision records. State the changed mathematical object, dimensions, invariants, limiting
cases, and whether comparison is basis- or subspace-dependent. Update all affected contracts and
add tests of the defining identities. Return one root-relative unified Git patch plus validation
results and direct Git commands.
