Read `.llm/state.md`, `.llm/project.md`, `.llm/development.md`, and the affected source/tests first.
Refactor without changing public behavior or widening supported scope. Identify the ownership
boundary being improved, demonstrate preservation through focused equivalence/regression tests,
update architecture navigation when responsibilities move, and return one root-relative unified
Git patch plus validation results and direct Git commands.
